import popupStyles from "./popupUI.module.css";
const PopupUI = () => {
  return (
    <li className={`popup-item ${popupStyles.container}`}>
      <span className={`popup-contents ${popupStyles.contentsContainer}`}>
        <p className={`${popupStyles.text}`}>This is a poppup!</p>
      </span>
    </li>
  );
};

export default PopupUI;
